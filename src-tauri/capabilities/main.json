{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the main Almanac window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
